#!/bin/sh
set -e

PRIVACY_FILE="src/content/privacy-policy.md"

if git diff --cached --name-only | grep -q "^${PRIVACY_FILE}$"; then
  node scripts/update-privacy-policy-date.mjs
  git add "${PRIVACY_FILE}"
fi
